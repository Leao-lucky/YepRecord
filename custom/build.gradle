plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.yep.record.custom'
}

apply from: rootProject.file("flavor.gradle")
apply from: rootProject.file("common.gradle")

dependencies {
    implementation project(':common')
}